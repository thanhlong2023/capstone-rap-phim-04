import { styled } from "styled-components";

export const Title = styled.h2`
  font-size: 4rem;
  font-weight: 400;

  color: #ffffff;
  background: orange;

  max-width: 70rem;
  padding: 1.2rem 0 1.2rem 2.5rem;
`;
